# 작업 결정 기록

- 배포 없이 파일 공유로 사용할 수 있도록 외부 의존성 없는 `index.html` 한 장으로 만든다.
- 후보와 확률은 UI에 표시하지 않으며, JavaScript에서 50%·25%·25% 비중으로 선택한다.
- 마인크래프트 원본 리소스나 상표 이미지는 쓰지 않고, CSS 블록 패턴과 픽셀풍 도형으로 분위기를 만든다.
- `node`로 내장 JavaScript 문법과 필수 문구를 확인했다.
- `background.jpg`는 홈의 전체 배경으로, `dd-cutout.png`는 배경을 제거한 오른쪽 인물 장식으로 사용한다.
- 결과가 나올 때까지 후보 이름을 화면에 표시하지 않도록 추첨 중에는 물음표 블록만 순환한다.
- 홈 화면과 뽑기 화면은 한 HTML 파일에서 표시 상태만 전환하므로 파일 공유 방식도 그대로 유지된다.
- 이번 조정은 MINECRAFT 단어를 제목의 중심 요소로 키우고, 뽑기 화면의 나무 상자 질감을 블록 기반 질감으로 바꾸는 데 집중한다.
- 원격 저장소는 비어 있어, 현재 작업 폴더에서 새 Git 저장소를 초기화한 뒤 완성 파일을 첫 커밋으로 올린다.
- 첫 커밋 `b58c350`을 `main` 브랜치로 푸시했다. 원본 `dd.jpg`와 중간 산출물 `dd-chroma.png`는 커밋하지 않았다.
